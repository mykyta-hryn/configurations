import { __exports as Folder$1 } from '../../../../_virtual/Folder.js';
import '../../../lodash/lodash.js';
import '../../../gzip-size/index.js';
import './Module.js';
import './BaseFolder.js';
import './ConcatenatedModule.js';
import './utils.js';
import { __exports as Module } from '../../../../_virtual/Module.js';
import { __exports as BaseFolder } from '../../../../_virtual/BaseFolder.js';
import { __exports as ConcatenatedModule } from '../../../../_virtual/ConcatenatedModule.js';
import { __exports as utils } from '../../../../_virtual/utils2.js';
import { l as lodash } from '../../../../_virtual/lodash.js';
import { g as gzipSize } from '../../../../_virtual/index3.js';

Object.defineProperty(Folder$1, "__esModule", {
  value: true
});
Folder$1.default = void 0;

var _lodash = _interopRequireDefault(lodash.exports);

var _gzipSize = _interopRequireDefault(gzipSize.exports);

var _Module = _interopRequireDefault(Module);

var _BaseFolder = _interopRequireDefault(BaseFolder);

var _ConcatenatedModule = _interopRequireDefault(ConcatenatedModule);

var _utils = utils;

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

class Folder extends _BaseFolder.default {
  get parsedSize() {
    return this.src ? this.src.length : 0;
  }

  get gzipSize() {
    if (!_lodash.default.has(this, '_gzipSize')) {
      this._gzipSize = this.src ? _gzipSize.default.sync(this.src) : 0;
    }

    return this._gzipSize;
  }

  addModule(moduleData) {
    const pathParts = (0, _utils.getModulePathParts)(moduleData);

    if (!pathParts) {
      return;
    }

    const [folders, fileName] = [pathParts.slice(0, -1), _lodash.default.last(pathParts)];
    let currentFolder = this;
    folders.forEach(folderName => {
      let childNode = currentFolder.getChild(folderName);

      if ( // Folder is not created yet
      !childNode || // In some situations (invalid usage of dynamic `require()`) webpack generates a module with empty require
      // context, but it's moduleId points to a directory in filesystem.
      // In this case we replace this `File` node with `Folder`.
      // See `test/stats/with-invalid-dynamic-require.json` as an example.
      !(childNode instanceof Folder)) {
        childNode = currentFolder.addChildFolder(new Folder(folderName));
      }

      currentFolder = childNode;
    });
    const ModuleConstructor = moduleData.modules ? _ConcatenatedModule.default : _Module.default;
    const module = new ModuleConstructor(fileName, moduleData, this);
    currentFolder.addChildModule(module);
  }

  toChartData() {
    return { ...super.toChartData(),
      parsedSize: this.parsedSize,
      gzipSize: this.gzipSize
    };
  }

}

Folder$1.default = Folder;
